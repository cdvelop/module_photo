package photo

import (
	"context"
	// "time"
	// "github.com/chromedp/chromedp"
)

func TomarFoto(ctx context.Context) error {
	// // Hace clic en el enlace con el selector ".menu-container a[name='photo']"
	// if err := chromedp.Run(ctx, chromedp.Click(".menu-container a[name='photo']")); err != nil {
	// 	return err
	// }

	// if err := chromedp.Run(ctx, chromedp.Sleep(1*time.Second)); err != nil {
	// 	return err
	// }

	// // Espera a que aparezca el botón con el selector "button[name='btn_add']" dentro del elemento con el id "photo"
	// if err := chromedp.Run(ctx, chromedp.WaitVisible("#photo button[name='btn_add']")); err != nil {
	// 	return err
	// }

	// // Hace clic en el botón con el selector "button[name='btn_add']" dentro del elemento con el id "photo"
	// if err := chromedp.Run(ctx, chromedp.Click("#photo button[name='btn_add']")); err != nil {
	// 	return err
	// }

	// // Espera 5 segundos para que la cámara se encienda
	// if err := chromedp.Run(ctx, chromedp.Sleep(1*time.Second)); err != nil {
	// 	return err
	// }

	// // Hace clic en el botón con el selector "button[name='btn_save']"
	// if err := chromedp.Run(ctx, chromedp.Click("#photo button[name='btn_save']")); err != nil {
	// 	return err
	// }

	return nil
}
